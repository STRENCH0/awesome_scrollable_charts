name: awesome_scrollable_charts
description: Beautiful, animated, and highly customizable scrollable charts for Flutter. Features line charts and stacked area charts with smooth animations and extensive styling options.
version: 0.1.0
homepage: https://github.com/strench0/awesome_scrollable_charts
repository: https://github.com/strench0/awesome_scrollable_charts

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
